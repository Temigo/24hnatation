'use strict';

/**
 * @ngdoc function
 * @name v24hApp.controller:ReglementCtrl
 * @description
 * # ReglementCtrl
 * Controller of the v24hApp
 */
angular.module('v24hApp')
  .controller('ReglementCtrl', function ($scope, $rootScope) {
      $rootScope.pactive = 'reglement';
  });
